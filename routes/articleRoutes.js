// initialize with models
const { Articles } = require('../models')

// module.exports with routes pushing to index
module.exports = app => {

    // GET all articles 
    // app.get

    // POST one article
    // app.post

}